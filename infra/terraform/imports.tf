# Import blocks removed — imports are handled as resilient shell commands
# in .github/workflows/terraform.yml (bulk-import step with || true).
# This prevents one missing resource from halting all other provisioning.
